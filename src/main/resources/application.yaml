spring:
  application:
    name: portfolio-application
  datasource:
    url: jdbc:h2:mem:testdb
    username: root
    password:

  h2:
    console.enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,loggers,beans,mappings
#  endpoint:
#    health:
#      show-details: always

info:
  app:
    name: Portfolio API
    description: Developer portfolio backend
    version: 1.0.0