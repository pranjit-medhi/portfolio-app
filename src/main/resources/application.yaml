spring:
  application:
    name: portfolio-application
  datasource:
    url: jdbc:h2:mem:testdb
    username: root
    password:

  h2:
    console.enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087